# Copy to .env and fill in locally. NEVER commit .env.
# NOTE: The rebuilt platform uses ONLY public market-data endpoints.
# No API credentials are required for historical download, training,
# evaluation, or live paper trading. These placeholders exist only for
# forward compatibility and remain unused by the current codebase.
BINANCE_API_KEY=
BINANCE_API_SECRET=
