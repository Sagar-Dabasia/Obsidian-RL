"""Live-paper trading: frozen policies, finalized candles, simulated execution only.

This package can never place an exchange order: it contains no authenticated client,
no order endpoint, and no credentials access.
"""
